# 빌드 산출물
build/
.gradle/
out/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# 테스트는 컨테이너 빌드 단계에서 안 돌리므로 (CI 가 이미 검증) 굳이 들고가지 않을 필요 없음
# - src/test 는 /workspace/src 로 복사되지만 -x test 로 실행 스킵

# 시크릿 / 환경 파일은 절대 이미지에 들어가면 안 됨
.env
.env.*
!.env.example

# git 메타
.git/
.gitignore
.gitattributes

# 문서 (이미지 사이즈 감소)
*.md
docs/
HELP.md
README.md
CLAUDE.md

# 도커 자체
Dockerfile
.dockerignore
docker-compose*.yml
